import 'package:flutter/widgets.dart';

class ScreenShorts extends StatelessWidget {
  const ScreenShorts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Screen Shorts'),
    );
  }
}
