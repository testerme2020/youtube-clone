import 'package:flutter/widgets.dart';

class ScreenSubscriptions extends StatelessWidget {
  const ScreenSubscriptions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Screen subscriptions"),
    );
  }
}
